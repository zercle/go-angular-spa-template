apiVersion: v1
kind: Service
metadata:
  name: go-angular-spa-template
spec:
  selector:
    app.kubernetes.io/name: go-angular-spa-template
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
