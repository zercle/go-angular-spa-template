# EXAMPLE ONLY — do NOT commit real secrets.
#
# This file is intentionally excluded from kustomization.yaml so it is never
# applied as-is. Supply the real Secret out-of-band via one of:
#   - an overlay patch that injects values from your secret store, or
#   - an external secret manager (e.g. External Secrets Operator, Sealed
#     Secrets, Vault Agent, or your cloud provider's CSI secret driver).
#
# The Deployment references a Secret named "server-secrets" with these keys.
apiVersion: v1
kind: Secret
metadata:
  name: server-secrets
type: Opaque
stringData:
  DB_PASSWORD: "change-me"
  VALKEY_PASSWORD: ""
